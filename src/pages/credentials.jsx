import React from "react";
import Form from "../components/credentials/Form";

const Credentials = () => {

    return (
        <div>
            <Form/>
        </div>
    )
}

export default Credentials;